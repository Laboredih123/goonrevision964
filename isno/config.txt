# log OOC channel
LOG_OOC

# log client Say 
LOG_SAY

# log admin actions
LOG_ADMIN

# log client access (logon/logoff)
LOG_ACCESS

# log game actions (start of round, results, etc.)
LOG_GAME

# log player votes
LOG_VOTE

# default probablity is 0, increase to make that mode more likely to be picked
# set to 0 to disable that mode
# note that, with under 10 players, multitraitor is exactly equivalent to traitor.
PROBABILITY TRAITOR 10
PROBABILITY MULTITRAITOR 10
PROBABILITY REVOLUTION 2
PROBABILITY SPYVSSPY 10
PROBABILITY METEOR 0
PROBABILITY BLOB 0
PROBABILITY FREEFORM 0

# allow players to initiate a restart vote
ALLOW_VOTE_RESTART

# allow players to initate a mode-change start
ALLOW_VOTE_MODE

# min delay (seconds) between voting sessions (default 10 minutes)
VOTE_DELAY 600

# time period (seconds) which voting session will last (default 1 minute)
VOTE_PERIOD 60

# prevents dead players from voting or starting votes
#NO_DEAD_VOTE

# players' votes default to "No vote" (otherwise,  default to "No change")
DEFAULT_NO_VOTE

# allow AI job
# DISALLOW_AI

# goon authentication
# AUTHENTICATION

RANDOM_NAMES 1

LOG_FILE logs/logROUNDNUMLOGTYPE.txt

# NO_RESPAWN